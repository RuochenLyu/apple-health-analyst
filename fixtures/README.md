# Test fixtures

Every Apple Health XML and ZIP file in this directory is an artificial test
fixture. Names such as `Riley’s Watch` and the recorded values were invented to
exercise parsing, source selection, incomplete sleep, and multi-device edge
cases. They do not contain or derive from a real person’s health export.
